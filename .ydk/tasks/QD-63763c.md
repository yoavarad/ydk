---
id: QD-63763c
title: fix(config): ANTHROPIC_API_KEY not loaded from .env — file is UTF-16LE encoded, dotenv loader expects UTF-8 so the var never reaches os.environ
status: done
type: quickdev
---

fix(config): ANTHROPIC_API_KEY not loaded from .env — file is UTF-16LE encoded, dotenv loader expects UTF-8 so the var never reaches os.environ
