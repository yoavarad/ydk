# ydk-core-schemas

Built-in YDK component schemas. These define the YAML structure for entities,
routes, hooks, events, contracts, crosscuts, and other component types.

Shipped with YDK. See `src/ydk/schemas/` for the actual schema files.
