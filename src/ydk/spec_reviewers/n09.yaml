id: N09
name: Component References
group: quality
threshold: 7
model_tier: smart

tools:
  - name: scan_unlinked_mentions
    description: Scan for mentions of entities/routes/errors without [ydk:...] tags.
    code: |
      def scan_unlinked_mentions(text: str, component_types: str = "entity,route,error,req,ext") -> str:
          import json, re
          findings: list[dict[str, object]] = []
          for line_no, line in enumerate(text.splitlines(), 1):
              cleaned = re.sub(r"\[ydk:[^\]]+\]", "", line)
              for m in re.finditer(r"\b([A-Z][a-z]+(?:[A-Z][a-z]+)+)\b", cleaned):
                  term = m.group(1)
                  if term not in {"GitHub", "JavaScript", "TypeScript", "PascalCase", "CamelCase"}:
                      findings.append({
                          "line": line_no,
                          "text": term,
                          "category": "unlinked_entity",
                          "suggestion": f"Consider [ydk:entity:.../{term}]",
                      })
              findings.extend([
                  {
                      "line": line_no,
                      "text": m.group(),
                      "category": "unlinked_route",
                      "suggestion": "Consider [ydk:route:...]",
                  }
                  for m in re.finditer(r"(GET|POST|PUT|PATCH|DELETE)\s+/[\w/{}.\-]+", cleaned)
              ])
              if re.search(r"\berror\b", cleaned, re.IGNORECASE) and re.search(
                  r"(returns?|throws?|raises?|produces?)\s+\w*\s*error", cleaned, re.IGNORECASE
              ):
                  findings.append({
                      "line": line_no,
                      "text": cleaned.strip()[:80],
                      "category": "unlinked_error",
                      "suggestion": "Consider [ydk:error:...]",
                  })
          return json.dumps(findings, indent=2)

system_prompt: |
  You are reviewing whether every mention of a specific entity, route, error, requirement, or system component uses an [ydk:...] reference.

  WHAT TO CHECK:
  1. Every specific entity/model mentioned by name has an [ydk:entity:...] reference
  2. Every specific API endpoint mentioned has an [ydk:route:...] reference
  3. Every specific error condition mentioned has an [ydk:error:...] reference
  4. Every specific business rule mentioned has an [ydk:req:...] reference
  5. Every external system mentioned has an [ydk:ext:...] reference

  WHAT IS ALLOWED WITHOUT REFERENCES:
  - General descriptions: "the system processes orders" (generic, not specific)
  - Conceptual language: "authentication flow" (concept, not specific component)
  - Section headings and navigation text

  EXAMPLES OF BAD (MISSING REFERENCES):
  - "The Order entity stores all trade details." -> Should be [ydk:entity:orders/Order]
  - "The user calls the create order endpoint." -> Should be [ydk:route:orders/create]

  EXAMPLES OF GOOD (PROPERLY REFERENCED):
  - "The [ydk:entity:orders/Order] stores all trade details."
  - "The user calls [ydk:route:orders/create] to place an order."


  CRITICAL RULES:
  1. ONLY report findings with EXACT quoted text from the document.
  2. Do NOT invent examples or fabricate findings.
  3. Every finding MUST include text that actually appears in the spec.
  4. If deterministic tools found 0 issues, that is strong evidence the document is clean.

  Use any available tools as evidence, then apply your judgment.

  SCORING RUBRIC:
  - Score 10: Every specific component mention has an [ydk:...] reference.
  - Score 8: Most references present, 1-2 minor omissions.
  - Score 5: References used inconsistently.
  - Score 3: Few references used.
  - Score 0: No [ydk:...] references used at all.

  Return ONLY valid JSON:
  {"score": N, "reasoning": "...", "suggestions": ["..."], "findings": [{"line": N, "text": "...", "issue": "..."}]}

examples:
  bad:
    - text: "The Order entity stores all trade details."
      issue: "Missing [ydk:entity:...] reference"
      fix: "The [ydk:entity:orders/Order] stores all trade details."
  good:
    - text: "The [ydk:entity:orders/Order] stores all trade details."

scoring:
  10: Every specific component mention has an [ydk:...] reference.
  8: Most references present, 1-2 minor omissions.
  5: References used inconsistently.
  3: Few references used.
  0: No [ydk:...] references used at all.
