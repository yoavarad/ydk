// Generated by ODK ignition — do not edit manually
export const queryKeys = {
{% for domain in domains %}
  {{ domain.name }}: {
    all: ['{{ domain.name }}'] as const,
    list: (params?: Record<string, unknown>) => ['{{ domain.name }}', 'list', params] as const,
    detail: (id: number | string) => ['{{ domain.name }}', id] as const,
{% for extra in domain.extra_keys %}
    {{ extra.name }}: ({{ extra.params }}) => ['{{ domain.name }}', {{ extra.key_parts }}] as const,
{% endfor %}
  },
{% endfor %}
} as const
