# odk-core-schemas

Built-in ODK component schemas. These define the YAML structure for entities,
routes, hooks, events, contracts, crosscuts, and other component types.

Shipped with ODK. See `src/odk/schemas/` for the actual schema files.
